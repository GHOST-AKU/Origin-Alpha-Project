# 决策记录

## 2026-08-07

### OAP-DEC-0001 项目定性

- 决定：Origin Alpha Project 是一个规模很大、长期推进、覆盖多种媒介的艺术创作计划。
- 影响：后续需要使用项目总纲、任务队列、决策记录和状态快照进行统一管理；不能按单一小说或单个 demo 管理。
- 状态：已确认项目规模与媒介属性；具体世界观、媒介关系和首阶段目标待确认。

### OAP-DEC-0002 基础目录方案

- 决定：采用混合结构：根目录放控制文件，`works/` 管理作品，作品内部按媒介划分；公共素材、研究资料、发布物分别独立管理。
- 影响：未来可以增加作品和媒介，而不必重构整个仓库。
- 状态：已批准；后由 `OAP-DEC-0003` 细化为多层项目容器。

### OAP-DEC-0003 多层项目容器方案

- 决定：Git 仓库作为整个项目容器；`vault/` 作为 Obsidian Vault；`sites/` 作为配套网站层；`other/` 作为实验、工具和其他项目层。
- 影响：小说知识、网站工程和其他项目彼此隔离，但仍可在同一仓库中统一版本管理。
- 风险：同一内容可能在 Vault、网站和发布物之间产生重复；后续必须通过稳定 ID 和发布记录管理单一来源。
- 回滚：删除新增空目录并恢复原占位目录即可，不影响现有创作内容。
- 状态：已批准，结构迁移已完成；目录名称后由 `OAP-DEC-0005` 修订。

### OAP-DEC-0004 项目规范说明书

- 决定：使用根目录的 `PROJECT_MANUAL.md` 作为长期规范说明书，记录项目分层、编号、文件命名、状态机、图表、工具分工和变更闸门。
- 影响：用户和后续协作者以同一份文件理解项目；稳定 ID、正典状态和单一来源规则成为长期基础设施。
- 风险：规范可能随着项目规模增长而需要版本化调整；任何规则变化必须记录决策并更新说明书版本。
- 回滚：保留旧版本并在 Git 中回退，或通过新的决策修订规则。
- 状态：`v0.1` 已于 2026-08-07 获用户确认，正式生效。

## 2026-08-09

### OAP-DEC-0005 创作资料目录采用工具无关命名

- 决定：将 `vault/` 改名为 `creative/`。`creative/` 是普通创作资料目录，Obsidian 可以把它作为 Vault 打开，但项目结构不以软件术语命名。
- 影响：正式小说、正典、作品资料、研究、人物、时间线和图表的根路径统一由 `vault/` 变为 `creative/`；说明书升级为 `v0.2`。
- 风险：旧路径引用或外部快捷方式可能失效；实际创作内容没有发生语义变化。
- 回滚：将 `creative/` 改回 `vault/`，并恢复说明书、忽略规则和项目文件中的路径引用。
- 状态：已获用户批准并执行。

### OAP-DEC-0006 Obsidian 的标准打开范围

- 决定：整个项目根目录作为 Obsidian 的标准打开范围；`creative/` 是创作内容区，不是 Obsidian Vault 的边界。
- 影响：用户可以在同一界面看到项目规范、创作资料、网站说明、其他项目和发布记录；Obsidian 配置位于根目录的 `.obsidian/`，该目录被 Git 忽略。
- 风险：根目录内容较多时，Obsidian 文件列表和搜索范围会更大；可通过 Obsidian 的排除设置或文件夹约定减少干扰。
- 回滚：重新将 `creative/` 单独作为 Obsidian 打开，并恢复 `creative/.obsidian/` 忽略规则。
- 状态：已获用户批准并执行。

### OAP-DEC-0007 创作模板零件盒

- 决定：模板就地放入它所服务的内容目录，不设立独立模板区，也不设立虚拟作品或作品骨架目录。`creative/03_works/` 当前直接保留文字、人物、作品世界、时间线、研究、图表、视觉、音频、视频、互动和作品附件的已确认内容分区及对应起始模板。`creative/02_canon/` 只接收已确认的跨作品正典。
- 影响：用户打开任一已确定的内容区时可直接看到对应模板；项目级研究、图表和附件与作品专属资料分开；正典目录具有明确准入门槛。第一部真实作品成立时，再根据当时已确认的作品形态决定目录结构，不在当前阶段预设。
- 风险：如未来需要跨作品的人物草稿、专用音频、影像或互动工作流，现有模板可能不足；在媒介关系和第一阶段目标确认前不预先扩张。
- 回滚：可通过 Git 恢复修订前的目录和文档；此时尚无真实作品使用旧路径，不需要内容迁移。
- 状态：用户于 2026-08-09 批准完整修订方案，已执行。

### OAP-DEC-0008 完整品牌名

- 决定：`Origin Alpha Project` 三个单词共同构成不可拆分的完整品牌名，唯一正式缩写是 `OAP`。不得用 `Origin Alpha` 或 `OA` 指代品牌或项目。
- 影响：正文、标题、网页元数据、无障碍标签和对外文案统一使用完整名称或 `OAP`。稳定 ID 前缀的迁移由 `OAP-DEC-0009` 另行记录。
- 回滚：如未来需要建立正式简称，必须通过新决策明确其适用场景，不默认缩短。
- 状态：用户于 2026-08-09 明确确认，已生效。

### OAP-DEC-0009 稳定 ID 前缀迁移

- 决定：将项目现有和未来的全部稳定 ID 前缀从 `OA-*` 统一迁移为 `OAP-*`。
- 影响：作品、正典对象、研究、图表、来源、素材、网站、任务、决策和发布物的编号全部改用 `OAP-*`。所有控制文件、模板、登记表和网站说明已同步迁移。
- 风险：旧提交历史仍会保留 `OA-*` 文本；历史不重写，当前工作区不得再分配旧前缀。
- 回滚：只能通过新决策和全量反向迁移恢复，不允许两套前缀并存。
- 状态：用户于 2026-08-09 批准全部迁移，已执行。
